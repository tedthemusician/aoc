(ns aoc.core
  (:require [aoc.utils :as utils]
            [aoc.2020.17 :as day-17])
  (:gen-class))

(defn -main
  "Placeholder"
  [& args]
  (println "I'm just here so the repl doesn't complain."))
